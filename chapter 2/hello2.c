#include<stdio.h>

int main(){

    int a =(int) 2.9999;  // if we only write int a it does not work when type (int) irt means to convert a into integer forefully but not by rounding off
    printf("%d",a);
    return 0;


}