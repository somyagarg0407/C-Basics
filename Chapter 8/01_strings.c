#include <stdio.h>

int main() {
    //char st[]={'a','b','\0'};
    char sr[]="cbi";  //same as above
    //for(int i=0;i<4;i++){
        //printf("character is %c \n",sr[i]);   
    //}
    printf("%s",sr); //here we can print whole string without using loop
    return 0; //also not use time to insert [] while pirinting string
}